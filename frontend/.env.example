# Prefer same-origin proxy routes in both dev and production
VITE_API_BASE_URL=/api

# Optional: set only if frontend and backend are hosted on different origins
# VITE_API_URL=https://api.yourdomain.com

VITE_WHATSAPP_NUMBER=919999999999
